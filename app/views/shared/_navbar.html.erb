<nav class="navbar navbar-expand-lg bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">Ancient garden</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <%= render "shared/#{current_namespace}/navbar" %>
      </ul>
      <% url = current_namespace.eql?("admins") ? admins_products_path : categories_path %>
        <%= form_with url: url, method: :get, class: "d-flex me-2" do %>
          <%= text_field_tag :query,
              params[:query],
              class: "form-control",
              placeholder: "Type a keyword"
          %>
          <%= submit_tag "Search", name: "", class: "btn btn-outline-info" %>
        <% end %>
        <%= link_to profile_path do %>
          <%= image_tag(current_user&.avatar.variant(:nav), class: "avatar-bordered") %>
        <% end %>
    </div>
  </div>
</nav>
