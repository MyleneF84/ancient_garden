<%= simple_form_for([:admins, @admins_order]) do |f| %>
  <div class="form-inputs">
    <%= f.input :user %>
    <%= f.input :address %>
    <%= f.input :total %>
    <%= f.input :fulfilled %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
